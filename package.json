{
  "name": "chat-app",
  "version": "1.0.0",
  "description": "",
  "main": "server.js",
  "scripts": {
    "start": "npm run build && node server/server.js",
    "dev": "npm run build && gulp nodemon-watch",
    "build": "gulp build",
    "test": "mocha server/**/*.test.js",
    "test-watch": "nodemon --exec 'npm test'"
  },
  "engines": {
    "node": "8.3.0"
  },
  "author": "charisTheo",
  "license": "ISC",
  "dependencies": {
    "express": "^4.16.3",
    "socket.io": "^2.1.1",
    "web-push": "^3.3.5"
  },
  "devDependencies": {
    "del": "^5.0.0",
    "expect": "^23.6.0",
    "gulp": "^4.0.2",
    "gulp-babel-minify": "^0.5.0",
    "gulp-concat": "^2.6.1",
    "gulp-minify-css": "^1.2.4",
    "gulp-nodemon": "^2.4.2",
    "gulp-sourcemaps": "^2.6.5",
    "mocha": "^5.2.0",
    "nodemon": "^1.18.4",
    "workbox-build": "^4.3.1"
  }
}
